name: CI Test
on:
  pull_request:
    types: [opened, synchronize]
  issue_comment:
    types: [created]

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "run unit tests"'

  acceptance-tests:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "run acceptance tests"'
